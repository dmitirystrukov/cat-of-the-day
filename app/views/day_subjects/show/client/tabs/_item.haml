%article.style2
  %span.image
    = image_tag social_post.day_subject_image.url
  = link_to social_post.url, target: '_blank' do
    %h2
      = social_post.message
  %a.post-author{ href: '#' }
    = "Author (#{social_post.user.email})"
